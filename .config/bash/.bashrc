#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias nm="NetworkManager" # As this defaults to some sort of symbol program 'nm' 

# PS1='[]\$ '

# vim ft:bash
